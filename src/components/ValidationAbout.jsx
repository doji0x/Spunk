import React from 'react';
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription } from '@/components/ui/dialog';
export default function ValidationAbout({ open, onOpenChange }) {
  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="rounded-2xl border-border bg-card sm:max-w-lg"><DialogHeader><DialogTitle className="font-display text-2xl tracking-tight">Proof, not promises.</DialogTitle><DialogDescription>What a Validate check actually means.</DialogDescription></DialogHeader><div className="space-y-4 text-sm leading-relaxed text-muted-foreground"><p>We read finalized Solana mainnet accounts directly. For a transaction, we identify token mints in its accounts. For a token address, we check that mint directly.</p><p>We derive the Metaplex and LibrePlex inscription data accounts, confirm their program ownership, verify each link back to the mint, and decode the stored image bytes. We also inspect Solana v1 transaction payloads. No off-chain image URLs, IPFS files, or gateway images count as proof.</p><p><strong className="text-foreground">Current coverage:</strong> Metaplex, Solana v1, and LibrePlex inscriptions; PNG, JPEG, GIF, and WebP images up to 5 MB. Other protocols and formats are not verified. Paste a token mint address, not a wallet or token-holding account.</p><p>A check proves storage and linkage at lookup time—not that a token is safe or an image is original. Images with an active update authority can change. Network errors are never treated as invalid inscriptions.</p><a href="https://www.metaplex.com/docs/smart-contracts/inscription" target="_blank" rel="noreferrer" className="inline-block font-medium text-primary underline underline-offset-4">Read the inscription standard ↗</a></div></DialogContent></Dialog>;
}