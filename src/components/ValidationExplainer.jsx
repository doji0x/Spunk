import React from 'react';
import { ScanLine, Layers, BadgeCheck } from 'lucide-react';
export default function ValidationExplainer() {
  const steps = [{ icon: ScanLine, number: '01', title: 'Drop an address', description: 'A token mint or transaction.\nThat’s all we need.' }, { icon: Layers, number: '02', title: 'We read the chain', description: 'The real inscription data.\nNot a link to somewhere else.' }, { icon: BadgeCheck, number: '03', title: 'Get the proof', description: 'An on-chain image, verified.\nOr a well-deserved get rekt.' }];
  return <section id="how-it-works" className="mx-auto mt-14 grid max-w-[750px] grid-cols-1 gap-6 text-left sm:mt-16 sm:grid-cols-3 sm:gap-0">{steps.map(({ icon: Icon, number, title, description }, i) => <div key={number} className={`relative px-5 sm:px-7 ${i ? 'sm:border-l sm:border-[#dce1d5]' : ''}`}><div className="mb-4 flex items-center gap-3"><Icon size={20} strokeWidth={1.5} className="text-[#626f57]" /><span className="font-mono text-[9px] text-[#a2a997]">/ {number}</span></div><h3 className="text-[13px] font-semibold">{title}</h3><p className="mt-2 whitespace-pre-line text-xs leading-[1.8] text-[#868d7d]">{description}</p></div>)}</section>;
}