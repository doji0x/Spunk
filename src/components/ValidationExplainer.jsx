import React from 'react';
import { ScanLine, Layers, BadgeCheck } from 'lucide-react';
export default function ValidationExplainer() {
  const steps = [{ icon: ScanLine, number: '01', title: 'Drop an address', description: 'A token mint or transaction.\nThat’s all we need.' }, { icon: Layers, number: '02', title: 'We read the chain', description: 'The real inscription data.\nNot a link to somewhere else.' }, { icon: BadgeCheck, number: '03', title: 'Get the proof', description: 'An on-chain image, verified.\nOr a well-deserved get rekt.' }];
  return <section id="how-it-works" className="mx-auto mt-14 grid grid-cols-1 gap-3 text-left sm:mt-16 sm:grid-cols-3">{steps.map(({ icon: Icon, number, title, description }) => <div key={number} className="rounded-2xl border border-border bg-card/70 p-5"><div className="mb-5 flex items-center justify-between"><Icon size={20} strokeWidth={1.5} className="text-primary" /><span className="font-mono text-[9px] text-muted-foreground">/ {number}</span></div><h3 className="font-display text-sm font-semibold">{title}</h3><p className="mt-2 whitespace-pre-line text-xs leading-[1.8] text-muted-foreground">{description}</p></div>)}</section>;
}